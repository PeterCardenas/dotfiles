git:
  autoPush: true
  autoCommit: true
  autoAdd: true

edit:
  command: /usr/bin/nvim
  apply: true
  watch: true

merge:
  command: "nvim"
  args: ["-d", "-c", '"$wincmd w"', "-c", '"wincmd J"']

diff:
  command: "nvim"
  args: ["-d", "-c", '"$wincmd w"', "-c", '"wincmd J"']
