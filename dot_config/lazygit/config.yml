gui:
  # TODO: there is no fuzzy filtering for files
  filterMode: 'fuzzy'
  skipRewordInEditorWarning: true
  splitDiff: "always"
  scrollPastBottom: true
  animateExplosion: true
  authorColors:
    "Peter Cardenas": "yellow"
    "PeterCardenas": "yellow"
    "PeterPCardenas": "yellow"
    "peter-cardenas-ai": "yellow"
    "*": "white"
  nerdFontsVersion: '3'
  branchColors:
    "pcardenas": "yellow"
    "master": "#9ece6a"
    "main": "#9ece6a"
  theme:
    selectedLineBgColor:
      - "#374261"
    activeBorderColor:
      - "#ff9e64"
      - "bold"
    inactiveBorderColor:
      - "#29a4bd"
    searchingActiveBorderColor:
      - "#ff9e64"
      - "bold"
    optionsTextColor:
      - "#7aa2f7"
    cherryPickedCommitFgColor:
      - "#7aa2f7"
    cherryPickedCommitBgColor:
      - "#3901a0"
    markedBaseCommitFgColor:
      - "#7aa2f7"
    markedBaseCommitBgColor:
      - "#e0af68"
    unstagedChangesColor:
      - "#db4b4b"
    defaultFgColor:
      - "#c0caf5"
keybinding:
  # TODO: Push/pull branches that are not checked out
  files:
    commitChanges: 'C'
    commitChangesWithEditor: 'c'
  commits:
    moveDownCommit: '<c-n>'
    moveUpCommit: '<c-p>'
    renameCommit: 'R'
    renameCommitWithEditor: 'r'
git:
  parseEmoji: true
  overrideGpg: true
  # TODO: set --verbose when running commit editor
  # TODO: use branchPrefix but specific to repos
  # TODO: commitPrefix but specific to repos
  paging:
    colorArg: always
    pager: delta --dark --paging=never
# TODO: does not chnage bottom prompt when entering patch commit message, or use editor
# TODO: checkout freezes the gui
notARepository: 'skip'
promptToReturnFromSubprocess: false
disableStartupPopups: true
quitOnTopLevelReturn: true
os:
  editPreset: 'nvim-remote'
  edit: '[ -z "$NVIM" ] && (nvim -- {{filename}}) || (nvim --server "$NVIM" --remote-expr "execute(\"lua vim.cmd([[e {{filename}}]])\")")'
  editAtLine: '[ -z "$NVIM" ] && (nvim +{{line}} -- {{filename}}) || (nvim --server "$NVIM" --remote-expr "execute(\"lua vim.cmd([[e +{{line}} {{filename}}]])\")")'
  openDirInEditor: '[ -z "$NVIM" ] && (nvim -- {{dir}}) || (nvim --server "$NVIM" --remote-expr "execute(\"lua vim.cmd([[e {{dir}}]])\")")'
  copyToClipboardCmd: osc52_copy {{text}}
