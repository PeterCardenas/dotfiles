# Ref:
# https://github.com/mitchellh/ghostty/blob/main/src/config/Config.zig
config-file = ?config_macbook
config-file = ?config_linux

# Make full screen
window-height = 9999
window-width = 9999

gtk-titlebar = false

custom-shader = ./shaders/matrix-hallway.glsl
custom-shader = ./shaders/cubes.glsl
custom-shader-animation = always

selection-invert-fg-bg = true

window-decoration = false

window-padding-y = 0
window-padding-x = 0

keybind = ctrl+enter=unbind
keybind = ctrl+v=paste_from_clipboard
keybind = ctrl+q=quit
keybind = global:alt+t=toggle_quick_terminal
keybind = super+q=quit

# Underline is a bit high (especially for replace mode in fish shell).
adjust-underline-position = 2

auto-update = download

theme = "tokyonight-storm"
