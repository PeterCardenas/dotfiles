#!/bin/bash
cat > /dev/null
mkdir -p "$HOME/.claude/settings.d"
cat > "$HOME/.claude/settings.d/{{ base .chezmoi.sourceDir }}.json" <<'FRAGMENT'
{{ template "claude-settings.json" . }}
FRAGMENT
exec "$HOME/.claude/hooks/merge_settings_d.py"
